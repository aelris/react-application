import React from "react";

export const MovieInfo = ({movieCardList}:{movieCardList: {title: string, year: string, imageUrl: string, genre: string}[]}) => {
  return (
    <div className="movieInfoContainer">
      <img src={}/>
    </div>
  )
}